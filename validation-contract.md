# Validation contract — #257 (anti-drift parallelism: pre-spawn file-overlap partition)

> Written from the ticket's acceptance criteria (#255 dogfood), then filled with
> captured EVIDENCE (command + exit code + output snippet) before finalizing the PR.
> Dogfoods the #255 presence gate: `scripts/validate-contract.sh --file validation-contract.md`.
> Spec: docs/specs/2026-06-07-anti-drift-parallelism.md

| # | Assertion | Command | Expected | Verdict | Evidence |
|---|---|---|---|---|---|
| 1 | Partition logic is correct — the headline acceptance criterion: 3 tickets where 2 share a file → `parallel == [the disjoint one]` and `serialize_groups[0].group == [the 2 overlapping]` | `printf '%s' '{"tickets":[{"number":301,"title":"a","body":"scripts/status.sh"},{"number":302,"title":"b","body":"scripts/autopickup-tick.sh"},{"number":303,"title":"c","body":"scripts/autopickup-tick.sh"}]}' \| NO_COLOR=1 bash scripts/plan-parallel-batch.sh --json` | `.parallel == [301]` and `.serialize_groups[0].group == [302,303]` | PASS | ran it -> exit 0; `jq -c '{parallel, sg0: .serialize_groups[0].group}'` => `{"parallel":[301],"sg0":[302,303]}`. Disjoint 301 parallelized; overlapping 302/303 serialized together. |
| 2 | Partition logic is conservative — an unknown-footprint ticket (no path/keyword) is NOT silently parallelized; it lands on the `unknown` list | `printf '%s' '{"tickets":[{"number":401,"title":"do a thing","body":"no path here"}]}' \| NO_COLOR=1 bash scripts/plan-parallel-batch.sh --json` | `.parallel == []` and `.unknown == [401]` | PASS | ran it -> exit 0; `jq -c '{parallel, unknown}'` => `{"parallel":[],"unknown":[401]}`. Unknown footprint quarantined, not parallelized (cheaper error = serialize/flag). |
| 3 | Report-only — the helper RECOMMENDS a partition and never spawns workers, merges, labels, writes state, or commits | `grep -nE 'It does NOT spawn workers, merge, label, or mutate any' scripts/plan-parallel-batch.sh && ! grep -qnE 'Agent\(\|gh (issue\|pr)\|state/active\|git (commit\|push)' scripts/plan-parallel-batch.sh && echo REPORT_ONLY` | exit 0 / `REPORT_ONLY` | PASS | header line 18 asserts "It does NOT spawn workers, merge, label, or mutate any" state; a grep for `Agent(` / `gh issue\|pr` / `state/active` / `git commit\|push` returns no operational call (only descriptive comment text). Report-only confirmed. |
| 4 | Deterministic + offline — same input twice yields byte-identical JSON (modulo `generated_at`); no network/docker/auth needed | `IN='{"tickets":[{"number":301,"title":"a","body":"scripts/status.sh"},{"number":302,"title":"b","body":"scripts/autopickup-tick.sh"},{"number":303,"title":"c","body":"scripts/autopickup-tick.sh"}]}'; A="$(printf '%s' "$IN" \| NO_COLOR=1 bash scripts/plan-parallel-batch.sh --json \| jq 'del(.generated_at)')"; B="$(printf '%s' "$IN" \| NO_COLOR=1 bash scripts/plan-parallel-batch.sh --json \| jq 'del(.generated_at)')"; [ "$A" = "$B" ] && echo DETERMINISTIC` | `DETERMINISTIC` | PASS | ran it -> printed "DETERMINISTIC: byte-identical (modulo generated_at)". Two runs of identical input produced identical JSON after stripping `generated_at`. No network/docker/auth invoked. |
| 5 | Composes with worker-conflict-resolver — the resolver agent doc explicitly names this helper as the *preventive* half (helper before spawn) vs the resolver as the *curative* half (after collision) | `grep -qF 'scripts/plan-parallel-batch.sh' .claude/agents/worker-conflict-resolver.md && echo COMPOSES` | exit 0 / `COMPOSES` | PASS | ran it -> exit 0; the resolver agent gained a "Relationship to `scripts/plan-parallel-batch.sh` (#257)" paragraph: "you are the *curative* half … `plan-parallel-batch.sh` is the *preventive* half … complementary, not redundant". |
| 6 | Tests pass — the full assertion driver is green (33 assertions incl. the headline acceptance criterion) and the script is syntactically clean | `bash -n scripts/plan-parallel-batch.sh && bash scripts/test-plan-parallel-batch.sh` | exit 0 / "33 passed · 0 failed" | PASS | ran it -> exit 0; `bash -n` clean; driver summary "33 passed · 0 failed". |
| 7 | Self-test script kind has no NEW failures vs baseline (the only failure is the pre-existing `state-schemas`/ajv case; `self-test/run.sh` exits 1 by design when any case fails, so this row asserts the *summary count* instead of the runner exit) | `bash self-test/run.sh --kind script 2>&1 \| grep -qE 'passed · 1 failed' && echo BASELINE_HELD` | `BASELINE_HELD` | PASS | ran it -> exit 0, "BASELINE_HELD"; the only failing case is the pre-existing `state-schemas` SKIP_HAS_AJV, untouched by this branch. |
| 8 | CI syntax check is green (spec frontmatter + all scripts/agents parse) | `bash scripts/preflight-syntax.sh` | exit 0 / "syntax-check: OK" | PASS | ran it -> exit 0; "syntax-check: OK"; includes `ok docs/specs/2026-06-07-anti-drift-parallelism.md` + `scripts/plan-parallel-batch.sh`. |
| 9 | THIS ticket's own contract passes the #255 presence gate (dogfood) | `bash scripts/validate-contract.sh --file validation-contract.md` | exit 0 | PASS | ran it -> exit 0; "every row names a command, every PASS carries evidence" over 9 assertion rows. |
