{
  "_description": "Back-compat fixture for the repo-assignee-override self-test case (kind: script). Same shape as repos-with-override.json but with NO assignee_override field — Commander must still accept it (absent = @me default). Proves the schema patch is additive and doesn't break any existing repos.json that was happily valid pre-#26.",
  "default_ticket_trigger": "assignee",
  "repos": [
    {
      "owner": "fixture-org",
      "name": "legacy",
      "local_path": "/tmp/fixture/legacy",
      "enabled": true,
      "ticket_trigger": "assignee"
    }
  ]
}
