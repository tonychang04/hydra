# Validation contract — #260 (ticket_trigger optional+default + state migration path)

> Written from the ticket's acceptance criteria, then filled with captured EVIDENCE
> (command + exit code + output snippet) before finalizing the PR. Dogfoods the #255
> presence gate: `scripts/validate-contract.sh --file validation-contract-260.md`.
> (Worktree-local, gitignored — the committed `validation-contract.md` belongs to the
> prior merged #257; this ticket does not touch it.)
> Spec: docs/specs/2026-06-08-state-validation-migration.md

| # | Assertion | Command | Expected | Verdict | Evidence |
|---|---|---|---|---|---|
| 1 | RECONCILIATION (headline): a repos.json with NO `ticket_trigger` on an entry now VALIDATES strict — schema no longer requires it | `scripts/validate-state.sh --schema-dir state/schemas --schema state/schemas/repos.schema.json self-test/fixtures/state-schemas/migrate/repos.before.json` | exit 0 / "passes" | PASS | ran it -> exit 0; "✓ …/repos.before.json passes strict (ajv) validation against …/repos.schema.json". entry 1 has no ticket_trigger; previously this hard-blocked preflight. |
| 2 | `ticket_trigger` removed from the items `required` array; all other required fields kept | `jq -c '.properties.repos.items.required' state/schemas/repos.schema.json` | `["owner","name","local_path","enabled"]` | PASS | ran it -> `["owner","name","local_path","enabled"]`. ticket_trigger gone from required; owner/name/local_path/enabled intact. |
| 3 | Zero behavior change — every consumer already defaults absent `ticket_trigger` to `assignee` | `grep -REn 'ticket_trigger' scripts/hydra-mcp-server.py scripts/hydra-status.sh scripts/hydra-list-repos.sh` | matches with `// "assignee"` / `get(..., "assignee")` defaults | PASS | ran it -> hydra-mcp-server.py:355 `r.get("ticket_trigger", "assignee")`; hydra-status.sh:154 `(.ticket_trigger // "assignee")`; hydra-list-repos.sh:80 `(.ticket_trigger // $dt)` where `$dt = default_ticket_trigger // "assignee"`. No code change needed for the default. |
| 4 | RED: a citation with a NUMERIC `tickets[]` element fails ajv-strict validation | `scripts/validate-state.sh --schema-dir state/schemas --schema state/schemas/memory-citations.schema.json self-test/fixtures/state-schemas/migrate/citations.before.json` | exit 1, error names `tickets` / "must be string" | PASS | ran it -> exit 1; ajv error `instancePath: '/citations/…/tickets/1' … message: 'must be string'`. The exact #260 drift class. |
| 5 | GREEN: `--migrate` coerces numeric `tickets[]` to strings, then the repaired file validates clean | `TMP=$(mktemp -d); cp self-test/fixtures/state-schemas/migrate/citations.before.json "$TMP/memory-citations.json"; scripts/validate-state.sh --schema-dir state/schemas --migrate "$TMP/memory-citations.json"; jq -c '.citations[].tickets' "$TMP/memory-citations.json"` | migrate exit 0 + "passes"; tickets all strings | PASS | ran it -> "• --migrate: coerced 1 numeric tickets[] element to strings" + "✓ … passes strict (ajv) validation"; `jq` => `["#50","191","#64"]` (191 now a string). |
| 6 | GREEN: `--migrate` backfills missing `repos[].ticket_trigger` with `default_ticket_trigger // "assignee"`; explicit values untouched | `TMP=$(mktemp -d); cp self-test/fixtures/state-schemas/migrate/repos.before.json "$TMP/repos.json"; scripts/validate-state.sh --schema-dir state/schemas --migrate "$TMP/repos.json"; jq -c '[.repos[].ticket_trigger]' "$TMP/repos.json"` | entry1 backfilled "assignee", entry2 stays "label" | PASS | ran it -> "• --migrate: backfilled ticket_trigger on 1 repos entry"; `jq` => `["assignee","label"]`. Disjoint: backfilled the missing one, left the explicit `label` alone. |
| 7 | `--migrate` is idempotent — a second run yields a byte-identical file | `TMP=$(mktemp -d); cp self-test/fixtures/state-schemas/migrate/repos.before.json "$TMP/repos.json"; scripts/validate-state.sh --schema-dir state/schemas --migrate "$TMP/repos.json" >/dev/null 2>&1; A=$(jq -S . "$TMP/repos.json"); scripts/validate-state.sh --schema-dir state/schemas --migrate "$TMP/repos.json" >/dev/null 2>&1; B=$(jq -S . "$TMP/repos.json"); [ "$A" = "$B" ] && echo IDEMPOTENT_OK` | `IDEMPOTENT_OK` | PASS | ran it -> "IDEMPOTENT_OK". Second migrate is a no-op; already-present trigger / already-string tickets are untouched. |
| 8 | `--migrate` is a no-op on already-valid state (reports "no repairs needed", does not rewrite) | `TMP=$(mktemp -d); cp self-test/fixtures/state-schemas/valid/repos.json "$TMP/repos.json"; scripts/validate-state.sh --schema-dir state/schemas --migrate "$TMP/repos.json" 2>&1 \| grep -c 'no repairs needed'` | `1` | PASS | ran it -> "• --migrate: no repairs needed for …/repos.json (already conforms to documented defaults)"; grep -c => 1. |
| 9 | Plain validation (no `--migrate`) stays READ-ONLY — `--migrate` is the only write path | `grep -n 'mv "$final_tmp" "$state_file"' scripts/validate-state.sh; grep -n 'if \[\[ "$migrate" -eq 1 \]\]' scripts/validate-state.sh` | the only `mv` onto $state_file is inside the `migrate -eq 1` block | PASS | ran it -> the sole `mv "$final_tmp" "$state_file"` sits inside the `if [[ "$migrate" -eq 1 ]]` block; no other write to $state_file exists. Preflight (plain validate) never mutates state. |
| 10 | `validate-state-all.sh` is GREEN against the real state dir under ajv-strict (preflight no longer blocked) | `scripts/validate-state-all.sh` | exit 0 / "validate-state-all: OK" | PASS | ran it -> "validate-state-all: OK (2 passed, 12 skipped) [validator: ajv strict]". |
| 11 | Self-test `script` kind is FULLY GREEN under the CI condition (ajv NOT on PATH — the CI self-test job runs the bash+jq fallback) | `bash self-test/run.sh --kind=script` (ajv hidden from PATH) | "52 passed · 0 failed · 2 skipped" (2 skips = `requires_claude_cli` cases CI also skips) | PASS | ran it (ajv removed from PATH to mirror CI) -> "52 passed · 0 failed · 2 skipped"; the `state-schemas` case passes all 34 steps including my 8 #260 assertions. (Local runs WITH ajv falsely fail step-19 `--strict without ajv` since ajv is present — irrelevant to CI.) |
| 12 | CI syntax check is GREEN (new spec frontmatter + all scripts parse) | `bash scripts/preflight-syntax.sh` | exit 0 / "syntax-check: OK" | PASS | ran it -> exit 0; "syntax-check: OK"; includes `ok docs/specs/2026-06-08-state-validation-migration.md`. |
| 13 | CLAUDE.md edit (rebased into the #277/#292 router) stays well under the FAIL ceiling; NOTICE advisory only (non-blocking, exit 0) | `bash scripts/check-claude-md-size.sh --quiet` | exit 0, < 18000 ceiling | PASS | ran it -> exit 0; "CLAUDE.md: 15444/18000 (85%) — approaching ceiling". NOTICE band (advisory), well below WARN (95%=17100) and FAIL (18000). All #277/#292 content preserved; my 2 doc points re-applied terse. |
| 14 | THIS ticket's own contract passes the #255 presence gate (dogfood) | `bash scripts/validate-contract.sh --file validation-contract-260.md` | exit 0 | PASS | ran it -> exit 0 (final run: every row names a command, every PASS carries evidence over 14 rows). |
| 15 | CI ROOT-CAUSE FIX: step 32 (`--migrate` no-op) no longer SIGPIPE-races — the test piped `validate-state.sh` into `grep -q` under `pipefail`; grep closed the pipe on match, the script took SIGPIPE (141), pipefail propagated it so the step exited 1 on fast runners (CI). Fixed by capturing the script output to a var, then grepping the var. | 20x loop of the new step-32 cmd (capture-then-grep), counting non-zero exits | 0 failures across 20 runs (deterministic NOOP_OK) | PASS | OLD piped form: 20/20 EXIT=141 (SIGPIPE 128+13). NEW capture-then-grep form: 0/20 failures, every run emits NOOP_OK exit 0. Production `validate-state.sh` untouched — only the golden-case test step changed. Also `--kind=script` whole suite green 52/0/2 under no-ajv (CI condition). |
