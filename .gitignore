# Project-specific knowledge (Phase 2: moves to S3; Phase 1: local-only, never committed)
memory/learnings-*.md
!memory/learnings-*.example.md
memory/archive/

# Runtime state
state/active.json
state/budget-used.json
state/memory-citations.json
state/quota-health.json
state/repos.json
state/linear.json
state/autopickup.json
state/commander.lock
state/pending-dispatches.json
state/slack-notified-prs.json
state/discord-notified-prs.json
state/webhook-triggers/
state/digests.json

# Daily digest runtime artifacts (spec: docs/specs/2026-04-17-daily-digest.md)
memory/digests/

# Connector configs — real values live here; only the .example mirror is committed.
state/connectors/*.json
!state/connectors/*.example.json

# Live operational artifacts
logs/*
!logs/.gitkeep
PAUSE
.hydra.env
hydra

# Phase 2 (cloud knowledge store) — real env is per-operator, contains
# bucket + region + table names. Template committed as .example.
.env.hydra-phase2

# Operator-specific config with absolute paths
.claude/settings.local.json
!.claude/settings.local.json.example

# Self-test cases reference specific repos on the operator's machine
self-test/golden-cases.json
!self-test/golden-cases.example.json

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Node (in case test-discovery runs npm)
node_modules/
